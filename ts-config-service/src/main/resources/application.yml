spring:
    application:
      name: ts-config-service
    data:
      mongodb:
        host: ts-config-mongo
        database: ts
        port: 27017
    zipkin:
      baseUrl: http://zipkin:9411/
    sleuth:
      sampler:
        percentage: 1.0

# HTTP Server
server:
  port: 15679  # HTTP (Tomcat) port
