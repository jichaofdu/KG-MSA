# HTTP Server
server:
  port: 14569 # HTTP (Tomcat) port

spring:
  application:
    name: ts-preserve-other-service
  zipkin:
    baseUrl: http://zipkin:9411/
  sleuth:
    sampler:
      percentage: 1.0
