spring:
  application:
    name: ts-station-service
  data:
    mongodb:
      host: ts-station-mongo
      database: ts
      port: 27017
  zipkin:
    baseUrl: http://zipkin:9411/
  sleuth:
    sampler:
      percentage: 1.0

# HTTP Server
server:
  port: 12345  # HTTP (Tomcat) port